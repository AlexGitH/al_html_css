SHORT DESCRIPTION

This is a root directory of the site with its default landing page(index.html).
Copy files and directories in it to the web server(Apache,nginx i.e.) to see favicon in the browser.

There is a table of content with references to all completed tasks grouped by corresponding parts.

Each item in the list is a link which configured to open its target in a new window/tab.

Each link name reflects relative path to corresponding "html" file source code.
